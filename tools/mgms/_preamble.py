# Things common to all MGMs

import logging    
    
# set up the basic logging    
logging.basicConfig(format="%(asctime)s [%(levelname)-8s] (%(filename)s:%(lineno)d:%(process)d)  %(message)s")
                    