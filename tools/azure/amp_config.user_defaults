cloud:
    azure:        
        account_name: video indexer account name
        client_id: client id
        client_secret: client secret
        tenant_id: tenant id
        subscription_id: subscription id
        resource_group: resource group
        
mgms:
    azure_video_indexer:
        s3_bucket: my-bucket
        